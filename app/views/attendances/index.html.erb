<h1 class="text-center" style="margin: 10px"> Ils participeront avec toi à ton event : </h1>

<% @participants.each do |participant| %>
<div class="container text-center">
  <div class="row">
    <div class="col-lg" style="max-width: auto;">
      <div class="card border-warning m-4" style="height: 10rem">
        <div class="card-header"><%= participant.first_name %> <%= participant.last_name %></div>
          <div class="card-body">
            <h6 class="card-title">Voici l'email de contact de <%= participant.first_name %> : <%= participant.email %></h6>
            <p class="card-text"><%= participant.description %></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>
  